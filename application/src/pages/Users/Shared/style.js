import styled from "styled-components";

export const BackgroundLoginContainer = styled.div`
    height: 100%;
    overflow: auto;
    background: var(--bg-login);
    display: flex;
    align-items: center;
`;